import React, { Fragment } from 'react';

const BadRequest = () => {
    return (
        <Fragment>
            <h1>Sorry, the page you requested does not exit! :(</h1>
        </Fragment>
    )
};

export default BadRequest;